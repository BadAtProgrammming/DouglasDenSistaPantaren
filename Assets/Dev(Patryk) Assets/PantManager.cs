using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PantManager : MonoBehaviour
{

    public int pantpoint;
    public int pantbttl;

    // Start is called before the first frame update
    void Start()
    {
        // Kommer att skriva in om pant, UI och vart det ska användas senare. TBD
    }

    // Update is called once per frame
    void Update()
    {
        // TBD
    }
}
